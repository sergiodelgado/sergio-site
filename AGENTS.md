# AGENTS

- Nunca pushear a `main`. Siempre crear rama `codex/<slug>` y abrir PR.
- Tocar solo archivos indicados en cada tarea.
- Incluir en cada PR: título semántico, checklist de aceptación, lista de archivos modificados y diff-resumen.
- Respetar identación, no re-formatear todo el HTML.
- Validar que el sitio compile en Vercel (Preview verde) antes de marcar la tarea como lista.
