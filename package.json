{
  "name": "sergio-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve .",
    "lint:html": "html-validate index.html about.html projects.html contact.html case-crbb-pipeline.html case-crbb-participantes.html",
    "check:sitemap": "node scripts/check-sitemap.mjs",
    "check:privacy": "node scripts/check-public-data-privacy.mjs",
    "ci:test": "npm run lint:html && npm run check:sitemap && npm run check:privacy",
    "ci:lighthouse": "lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1",
    "html-validate": "^11.5.2"
  }
}
